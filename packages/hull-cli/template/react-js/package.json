{
  "scripts": {
    "dev": "hull dev",
    "dev:vite": "hull dev --vite",
    "build": "hull build",
    "build:vite": "hull build  --vite",
    "server": "hull server",
    "server:vite": "hull server --vite"
  },
  "dependencies": {
    "react": "^17.0.1",
    "react-dom": "^17.0.1"
  }
}
